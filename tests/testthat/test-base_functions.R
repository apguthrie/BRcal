test_that("multiplication works", {
  expect_equal(2 * 2, 4)
})

# check that in functions tests behave properly?

# check that LLO_LRT gives correct p-value for fivethirtyeight

# check that LLO_LRT gives correct p-value for random hockey

# check that params are consistent for fivethirtyeight

# check that params are consistent for random pundit

# check test stat is consistent for both
